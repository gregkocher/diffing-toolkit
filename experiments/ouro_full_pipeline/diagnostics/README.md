These rejected diagnostic hooks are not imported by production code. Actual SDPA inspection showed that the failing mask was already contiguous with unit strides; making it contiguous did not repair padded generation. The supported campaign configuration uses native single-prompt generation and leaves Ouro, its attention masks, and its SDPA backend unchanged.

The left-padded batch probe failed with default SDPA. Its decode-step mask was already contiguous with unit strides, but broadcast over a longer key sequence. Restricting SDPA to Flash and Math avoided the kernel exception but failed greedy parity against individual native generation for both test prompts. These probes are negative diagnostics. The production validation entry point is `../validate_native_single.py`.
